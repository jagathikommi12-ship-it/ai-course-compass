# Anon key only — this app signs users in the same way the React app does,
# via Supabase Auth. Never put the service_role key here.
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your-anon-key
API_BASE_URL=http://localhost:8000
